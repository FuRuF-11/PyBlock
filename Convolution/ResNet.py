import torch
import torch.nn as nn
import torch.nn.functional as F
import math

class ResNet(nn.Module):
    def __init__(self,in_channel,hidden_channel,output,layer):
        super().__init__()

    def forward(self,X):
        return